spring:
  application:
    name: cdeh-msa-dm-prdd-party
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/bdd_party
    username: postgres
    password: postgres
    pool:
      enabled: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    url: jdbc:postgresql://localhost:5432/bdd_party
    user: postgres
    password: postgres
    validate-on-migrate: true

